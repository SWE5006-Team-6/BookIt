# Connect to Supabase via connection pooling
DATABASE_URL=

# Direct connection to the database. Used for migrations
DIRECT_URL=

# Supabase Auth
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=
# Required for MFA (TOTP): use publishable key (preferred) or legacy anon key. Dashboard → Project Settings → API
SUPABASE_PUBLISHABLE_KEY=
# SUPABASE_ANON_KEY=   # legacy; use SUPABASE_PUBLISHABLE_KEY if you have it

NODE_ENV=development